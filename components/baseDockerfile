# TODO: Because each docker file is at least 100M+ size. We create this base image and the components docker on top of it.
# TODO: Name docker file in skaffold to allow component docker using this file as base.
# Reference: https://docs.docker.com/develop/develop-images/dockerfile_best-practices/

# Base docker file
# 1. install python
# 2. install common requirements

FROM python:3.9-slim as base

COPY requirements.txt requirements.txt
RUN pip install -r requirements.txt

